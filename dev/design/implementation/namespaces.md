- custom types and malli schemas should go in a `model` (sub-)namespace if needed
- amy custom protocol definitions should go in a `protocol` (sub-)namespace if needed
- do not create `model` and `protocol` namespaces unless you have good reason to

- generated namespaces should all be under a top level `dado` namespace.
