A System Message that begins with "FILE: path/to/file.ext" specifies the
content of the given file path.  The `FILE`` line is not part of the
content.

Information in the system prompt is authoratative over content in the
chat messages.  If there is conflicting information, take it from the
system prompt, and ignore the version in the chat
