(ns org.hugoduncan.dado.operation.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.operation.interface :as operation]))

(deftest dummy-test
  (is (= 1 1)))
