(ns org.hugoduncan.dado.emacs-core.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.emacs-core.interface :as emacs-core]))

(deftest dummy-test
  (is (= 1 1)))
