(ns dado.ai.prompt.core
  "Core implementation of the AI Prompt component.")

(defn read-template [template-name]
  ;; TODO: Implement reading template file
  )

(defn parse-template [template-content] 
  ;; TODO: Implement parsing template content
  )

(defn compose-templates [parsed-templates]
  ;; TODO: Implement composing templates in order
  )

(defn substitute-data [composed-template data]
  ;; TODO: Implement substituting data values into template
  )  

(defn construct-prompt [template-names data]
  (let [;; TODO: 
        ;; 1. Read and parse all specified templates
        ;; 2. Compose templates in specified order  
        ;; 3. Substitute values from data map
        ]
    ;; TODO: Return final constructed prompt string
    ))
