(ns org.hugoduncan.dado.nrepl-middleware.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.nrepl-middleware.interface :as nrepl-middleware]))

(deftest dummy-test
  (is (= 1 1)))
