(ns dado.document-retrieval.interface-test
  (:require
   [clojure.test :refer [deftest is testing]]
   [dado.document-retrieval.interface :as document-retrieval]))

(deftest document-retrieval-test
  (is (= 1 1)))
