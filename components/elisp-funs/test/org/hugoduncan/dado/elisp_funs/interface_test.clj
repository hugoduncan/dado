(ns org.hugoduncan.dado.elisp-funs.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.elisp-funs.interface :as elisp-funs]))

(deftest dummy-test
  (is (= 1 1)))
