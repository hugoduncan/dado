(ns org.hugoduncan.dado.clojure-fns.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.clojure-fns.interface :as clojure-fns]))

(deftest dummy-test
  (is (= 1 1)))
