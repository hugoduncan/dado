(ns org.hugoduncan.dado.clojure-target.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.clojure-target.interface :as clojure-target]))

(deftest dummy-test
  (is (= 1 1)))
