(ns org.hugoduncan.dado.clojure-ns.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.clojure-ns.interface :as clojure-ns]))

(deftest dummy-test
  (is (= 1 1)))
