(ns org.hugoduncan.dado.elisp-target.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.elisp-target.interface :as elisp-target]))

(deftest dummy-test
  (is (= 1 1)))
