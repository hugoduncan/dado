(ns org.hugoduncan.dado.chat-gpt.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.chat-gpt.interface :as chat-gpt]))

(deftest dummy-test
  (is (= 1 1)))
