(ns org.hugoduncan.dado.elisp-feature.interface-test
  (:require [clojure.test :as test :refer :all]
            [org.hugoduncan.dado.elisp-feature.interface :as elisp-feature]))

(deftest dummy-test
  (is (= 1 1)))
